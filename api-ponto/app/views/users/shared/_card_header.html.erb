<%# Task 23.8 — Cabeçalho do card de autenticação (sessions + passwords).
    Padrão visual AdminLTE 4 da tela legada (app/views/admin/sessions/new.html.erb)
    extraído para evitar duplicação entre as 3 views Devise novas. Locals:
    app_title (string) e subtitle (string), alimentados via I18n
    (devise.sessions.new.* / devise.passwords.new.* / devise.passwords.edit.*). %>
<div class="text-center mb-4">
  <i class="fas fa-shield-halved fs-1 text-primary"></i>
  <h3 class="mt-2"><%= app_title %></h3>
  <p class="text-muted"><%= subtitle %></p>
</div>