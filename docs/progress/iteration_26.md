# Iteration 26 — Port de Estilo basic8 (prelúdio dos scaffolds)

> Status: 🔵 Em Andamento — implementação 26.1–26.10 consolidada no commit `1a73a4d` (branch `feat/demanda-port-estilo-basic8-fase2`, no origin); aguardando Code Reviewer/Bug Finder + verificação visual manual | Planejada originalmente: Período: 2026-09-28 a 2026-10-09 | Goal: Portar TODO o delta de estilo do basic8 (helpers de contexto, partials `shared/*`, layout admin, assets/build e bibliotecas JS/UX) como pré-requisito visual dos scaffolds estilo basic8 (Sprint 25) | RFs: RF04, RF05, RF06, RF12, RF13, RF14, RNF01, RNF03, RNF05, RNF06

## Decisão de numeração (ler antes)

- Esta sprint é um **prelúdio/acompanhante da Sprint 25 (scaffolds)** do `docs/12-plano-implementacao/implementation_plan.md`. Como a Sprint 25 do plano **já está ocupada** com "Templates de scaffold + convenções de modelo", a sprint de estilo é numerada **26** (`iteration_26.md`).
- **Ordem de execução ≠ numeração:** a Fase 1 (partials `shared/*` + helpers) é **pré-requisito** dos templates de scaffold (RF04/RF05 — os templates renderizam `shared/*`). O Project Planner deve revisar o Gantt do plano (Sprints 25–28) para registrar a precedência/renumeração do piloto (**Sprint 26 atual do plano** → empurra para 27+). Sem ação de escrita no `implementation_plan.md` por parte desta sprint; reportado ao Orchestrator.
- **Dois trilhos de planejamento coexistem:** `implementation_plan.md` (canônico, Sprints 24–28) e `Frequencia/SPRINT-PLAN.md` (Sprint 24 = CSS/Estilo ✅9/9, 25 = partials layout, 26 = CRUD). Não realinhar agora; o delta entre trilhos é insumo do code review e do CTO.

## Pré-condição

- **Sprint 24 aceita** (✅ 2026-09-23) — gems basic8 (zutils, simple_form, ransack, pagy) no bundle.
- **Working tree estabilizado antes do início:** commits manuais pendentes da Sprint 24 (COMMIT_MODE=manual: branches 24.2–24.6, Bug 10) e merge/layout da 23.7 **finalizados** (ou HEAD consolidado) para as branches encadeadas da Sprint 26 partirem do estado limpo (padrão das Sprints 23/24/25).
- **Estado real verificado na investigação (2026-09-23):** `manifest.js` **sem** `link_tree ../builds` (build não entra em produção); `admin.html.erb` ainda com CDN AdminLTE 4 + OverlayScrollbars CSS + fontsource, título hardcoded "API Ponto TJPI", sidebar inline com links mortos (`href="#"`) e JS inline duplicado; `ApplicationRecord` sem `self.icon`; `breadcrumbs_on_rails` **não** está no Gemfile; `bootstrap-icons` **não** está no package.json; `application.scss` sem imports das libs do basic8 (toastr/tom-select/bootstrap-table/fileinput/ckeditor). As tarefas **começam confirmando o estado real do repo** ("investigue antes de alterar").

## Desenvolvedores

| Dev | Perfil | Foco |
|-----|--------|------|
| Code Specialist (Dev A) | Backend + UI (helpers, partials, layout, importmap/Stimulus) | Fase 1 (26.1–26.5) + 26.6 (breadcrumbs) + Fase 3 (26.10) |
| Code Specialist Assets/Build (Dev B) — **opcional** | Node/Sass/Propshaft/importmap | Fase 2 (26.7, 26.8, 26.9) em worktree paralela (arquivos disjuntos; ver skill `parallel-execution`) |

> Padrão: **1 dev (Dev A)**. Com 2 devs, Dev B assume 26.7/26.8/26.9 a partir do HEAD consolidado; **26.6 permanece com Dev A** (toca `ApplicationController`, mesma região da 26.1).

## Backlog

### FASE 1 — UI estrutural (port de partials/helpers/layout)

#### Tarefa 26.1 — Helpers de contexto do layout: `resource_human_name`, `resource_icon`, `set_configurations` + fallback `ApplicationRecord.icon`

- User Story: Como dev, quero os helpers do basic8 que nomeiam/iconeiam recursos e definem o contexto do app para que os partials `shared/*` (título, header, sidebar) e os scaffolds (Sprint 25) herdem a convenção sem duplicação por tela
- Rastreabilidade: PRD RF06/D1 (convenção `Model.icon`), RNF05, RN03 (pt-BR); fontes: `basic8/app/helpers/application_helper.rb`, `basic8/app/models/application_record.rb`, `basic8/app/controllers/application_controller.rb`
- Estimativa: 2 pontos | Atribuição: Dev A
- Dependências: Sprint 24 concluída (zutils no bundle)
- Critérios de aceite:
  - [x] `ApplicationHelper` ganha `resource_icon(controller_name)` — `constantize` → `klass.icon` se responder, senão fallback (padrão basic8 `"fa fa-circle"` com `rescue NameError`)
  - [x] `resource_human_name(controller_name, action_name)` — `klass.model_name.human.pluralize` com fallback `controller_name.capitalize` (respeita I18n pt-BR)
  - [x] `ApplicationRecord.icon` (e `icon_for(field)` se portado) com default `"fa fa-fw fa-cube"` (fiel ao basic8) — herdável pelos models pilotos (`EstacaoPonto`/`Regime`/`Versao`)
  - [x] `set_configurations` (before_action no `Admin::ApplicationController`) define `@app_name` (identidade atual "API Ponto TJPI" preservada), `@app_description`, `@app_icon = ApplicationRecord.icon`, `@menu = []` e `@static_menu` refletindo a navegação atual do layout admin SEM rotas mortas (itens com `permission`/`permission_check`/`active_test`/`children` no padrão basic8)
  - [x] Contexto API/presença (`ApplicationController` base e `Presenca::*`) intocado (RNF01/RNF06)
  - [x] Testes: helper (fallback por NameError + override por model com `self.icon`) e controller (`@static_menu` exposto após ação admin real)
  - [x] `rails zeitwerk:check` OK; suíte baseline sem novas falhas (731/2431/1); rubocop limpo
- Status: 🔵 Em Andamento (implementação concluída e validada — aguardando Code Reviewer/Bug Finder; COMMIT_MODE=manual: commits/MR pendentes de aprovação)

**Linha do Tempo:**

| Horário | O que foi feito | Resultado |
|---------|-----------------|-----------|
| 2026-09-24 (sessão) | Investigação do estado real: `ApplicationHelper` (já tinha Pagy::Frontend + zutils `eval_with_rescue`/`menu_activated?`), `ApplicationRecord` (só `primary_abstract_class`, sem icon), `ApplicationController` base = `ActionController::API` (intocado — RNF01), `Admin::ApplicationController` (require_login/check_authorization/23.7 intactos), layout admin inline com links mortos (`href="#"`), rotas reais e ability 23.5 | Estado confirmado; branch `feat/demanda-port-estilo-basic8-fase1` sincronizada com origin; contexto lido (governance/progress/iteration) |
| 2026-09-24 (sessão) | Port do basic8: `ApplicationRecord.icon`/`icon_for` (default `"fa fa-fw fa-cube"`); `ApplicationHelper#resource_icon`/`resource_human_name` (fallback `"fa fa-circle"`/`capitalize` com `rescue NameError`); `Admin::ApplicationController#set_configurations` (APP_NAME="API Ponto TJPI", `@app_icon = ApplicationRecord.icon`, `@menu = []`, `@static_menu` com 3 níveis: Dashboard/Registros/Usuários/Presença→Estaçao de Ponto→Explorar Estações+Versões, Relatório Mensal→3 relatórios, Frequência/Regimes/Direitos & Deveres/Gestores — item morto "Inicializar Estação" REMOVIDO; permissões por item no padrão basic8) | 3 arquivos de produção alterados; zero migrations; auth/ability intactos; contexto API/presença intocado |
| 2026-09-24 (sessão) | Testes: 6 novos no `application_helper_test.rb` (resource_icon herança/override/PORO-sem-icon/NameError ×2; resource_human_name plural/fallback) + 3 novos no `admin/application_controller_test.rb` (contexto exposto após ação real; sem rotas mortas; navegação real + estrutura basic8) | 23 runs / 100 asserts / 0 failures (helper+controller); suíte admin 125 runs / 620 asserts / 0 failures |
| 2026-09-24 (sessão) | Validação: `bin/rails zeitwerk:check`; `bin/rubocop` nos 5 arquivos alterados; suíte completa `bin/rails test` | zeitwerk All is good; rubocop 5 files / no offenses; suíte 740 runs / 2482 asserts / 1 falha = ÚNICA PRÉ-EXISTENTE de timezone (`presenca_endpoints_test.rb:187`) — sem novas falhas (baseline 731/2431/1) |

#### Tarefa 26.2 — Partial `shared/_sidebar` (menu dinâmico data-driven)

- User Story: Como admin, quero uma sidebar que reflita meu nível de permissão e destaque a tela ativa para navegar aos módulos sem links mortos
- Rastreabilidade: PRD RF05 (navegação admin), RNF05; fontes: `basic8/app/views/shared/_sidebar.html.erb` (94 linhas, suporta 3 níveis), `basic8/app/controllers/application_controller.rb` (`@static_menu`)
- Estimativa: 3 pontos | Atribuição: Dev A
- Dependências: 26.1 (`@static_menu` via `set_configurations`); usa `menu_activated?`/`eval_with_rescue` já portados (zutils)
- Critérios de aceite:
  - [x] `app/views/shared/_sidebar.html.erb` portado do basic8 (estrutura/classes AdminLTE 4: `app-sidebar`, `sidebar-brand`, `sidebar-wrapper`, `data-lte-toggle="treeview"`, 3 níveis de profundidade), iterando `@static_menu`
  - [x] Filtro por permissão em TODOS os níveis com `can? mi.dig(:permission), mi.dig(:permission_check)` (pai, filho, neto) — gestor/operador não veem itens fora da matriz de permissões (23.5)
  - [x] `menu_activated?` aplicado para `active`/`menu-open` (comportamento fiel, incluindo o truthy `"error"` já documentado no helper)
  - [x] **Nenhum item de menu com `url: "#"` SEM `children`** (remoção de links mortos); itens-pai apenas-seção mantêm o `<a href="#">` do padrão AdminLTE treeview (são headers de seção, não rotas mortas)
  - [x] Ícones dos itens na convenção `fa-*` (Font Awesome — consistência Frequencia; as views já migraram `bi-*`→`fa-*`); `data-enable-persistence` e demais atributos AdminLTE preservados
  - [x] Verificação estrutural/funcional: HTML renderizado em ação admin real (ex.: `estacoes_path`) com menu filtrado por role (admin vê tudo; operador só leitura)
  - [x] Suíte baseline sem novas falhas; rubocop limpo
- Status: 🔵 Em Andamento (implementação concluída e validada — aguardando Code Reviewer/Bug Finder; COMMIT_MODE=manual: commits/MR pendentes de aprovação)

**Linha do Tempo:**

| Horário | O que foi feito | Resultado |
|---------|-----------------|-----------|
| 2026-09-24 (sessão) | Investigação do estado real: fonte `basic8/app/views/shared/_sidebar.html.erb` (94 linhas — filtro `can?` apenas no pai/filho; **neto SEM filtro no fonte**), `@static_menu` da 26.1 confirmado (3 níveis; todos os itens com `permission`/`permission_check`/`active_test`; grupos `url:"#"` com children), ability 23.5 (admin `manage :all`; operador baseline `read :all`), `menu_activated?`/`eval_with_rescue` na `ApplicationHelper`, `sidebar_controller.js` (persistência), layout admin inline (nav-arrow `fas fa-chevron-right`, item morto "Inicializar Estação" já removido na 26.1) | Estado confirmado; branch `feat/demanda-port-estilo-basic8-fase1` com working tree da 26.1 pendente (COMMIT_MODE=manual) |
| 2026-09-24 (sessão) | Porta do partial `app/views/shared/_sidebar.html.erb` (criado; `app/views/shared/` ainda não existia): estrutura AdminLTE 4 íntegra (`app-sidebar` + `data-enable-persistence="true"`, `sidebar-brand` com `@app_icon`/`@app_name` → `root_path`, `sidebar-wrapper` → `nav.mt-2` → `ul[data-lte-toggle="treeview"]`, 3 níveis); **filtro `can?` no neto ADICIONADO** (gap do fonte corrigido conforme critério); `nav-arrow` em `fas fa-chevron-right` (convenção FA Frequencia — fonte usa `bi-*`); `aria-label` pt-BR "Navegação principal" (RNF03 — fonte usa "Main navigation"); `menu_activated?` para `active`/`menu-open`; headers de seção mantêm `<a href="#">` com `ul.nav-treeview` filho | 1 arquivo de produção criado; zero migrations; auth/ability intactos (RNF06/RN06); layout `admin.html.erb` NÃO tocado (composição é a 26.5) |
| 2026-09-24 (sessão) | Testes: 6 novos em `test/controllers/admin/sidebar_test.rb` — integração que renderiza o partial no `view_context` do controller após action admin REAL (`estacoes_path`/`dashboard_path`; esteira completa router→before_action→CanCan→action), SEM antecipar a 26.5: (1) estrutura AdminLTE 4 + marca + 3 níveis + `data-enable-persistence`; (2) convenção FA sem `bi-*`; (3) navegação real completa + zero links mortos (`href="#"` só em headers com `ul` filho; exatamente 3 headers); (4) `menu_activated?` em `estacoes_path` (neto ativo + avô/pai `menu-open` + pai `active`; Dashboard não ativo); (5) Dashboard ativo em `dashboard_path`; (6) filtro por role (admin vê Usuários; operador não — baseline read 23.7; estrutura intacta p/ operador) | 6 runs / 91 asserts / 0 failures; suíte admin 131 runs / 711 asserts / 0 failures |
| 2026-09-24 (sessão) | Validação: `bin/rails zeitwerk:check` (binstub — lição Sprint 23); `bin/rubocop` no arquivo de teste; suíte completa `bin/rails test` | zeitwerk All is good; rubocop 1 file / no offenses (partial `.erb` não lintado — parser Ruby do RuboCop não entende ERB, offenses seriam falsos positivos; mesma prática da 26.1); suíte 746 runs / 2573 asserts / 1 falha = ÚNICA PRÉ-EXISTENTE de timezone (`presenca_endpoints_test.rb`) — sem novas falhas (baseline 740/2482/1) |

#### Tarefa 26.3 — Partial `shared/_header` (navbar com marca central + dropdown de usuário com badges de roles)

- User Story: Como usuário logado, quero ver a marca do app no topo e meu nome/roles no dropdown para saber quem sou e qual meu nível de acesso
- Rastreabilidade: RNF05; fontes: `basic8/app/views/shared/_header.html.erb` (adaptado ao Frequencia: `logout_path`, `current_user.nome_completo`, theme toggle já existente); **decisão de convenção badges `text-bg-*`** (registrada aqui — reutilizada na 26.4)
- Estimativa: 2 pontos | Atribuição: Dev A
- Dependências: 26.1 (`@app_icon`/`@app_name`)
- Critérios de aceite:
  - [x] `_header.html.erb` portado: toggle sidebar (`data-lte-toggle="sidebar"` + `data-controller="sidebar"` — preserva `sidebar_controller.js` do Frequencia), **marca central** (`@app_icon` + `@app_name` via `link_to root_path`), dropdown de tema (`theme_controller` — movido sem duplicar markup)
  - [x] Dropdown do usuário: nome (`current_user.try(:nome_completo)` — model `User` do Frequencia; fallback), email quando houver, **badges de roles** iterando `(current_user.try(:roles) || [])` → `role.name` com **convenção `text-bg-*`** (ex.: `text-bg-primary`; mesma convenção no `_title`)
  - [x] Logout via `button_to logout_path, method: :delete` (rota real do Frequencia — RN06; adaptação do `destroy_user_session_path` do basic8)
  - [x] Sem duplicação de markup com o layout (extração limpa; o layout passa a renderizar o partial na 26.5)
  - [x] Teste estrutural no HTML admin real (presença dos elementos-chave: marca, dropdown, badges; usuário sem roles → dropdown sem quebrar)
  - [x] Suíte baseline sem novas falhas
- Status: 🔵 Em Andamento (implementação concluída e validada — aguardando Code Reviewer/Bug Finder; COMMIT_MODE=manual: commits/MR pendentes de aprovação)

**Linha do Tempo:**

| Horário | O que foi feito | Resultado |
|---------|-----------------|-----------|
| 2026-09-24 (sessão) | Investigação do estado real: fonte `basic8/app/views/shared/_header.html.erb` (89 linhas — marca central, theme dropdown com `theme#pick`, user dropdown com `bg-primary`, logout `destroy_user_session_path`), layout admin inline (theme toggle nas linhas 58–81; user dropdown legado com `current_user&.admin?`; JS inline), `sidebar_controller.js` (task 24.8 — toggle+persistência, conexão `click->sidebar#toggle`), `theme_controller.js` (herdado do `<html data-controller="theme">` — `_syncDropdowns` varre `.app-header .dropdown-menu`), model `User` (`nome_completo` NOT NULL, `email` nullable — Sprint 23, `roles` via Rolify), rotas reais (`logout` DELETE /logout → users/sessions#destroy RN06; `root` GET / → admin/dashboard) | Estado confirmado; branch `feat/demanda-port-estilo-basic8-fase1` com working tree das 26.1/26.2 pendente (COMMIT_MODE=manual) |
| 2026-09-24 (sessão) | Porta do partial `app/views/shared/_header.html.erb` (criado junto ao `_sidebar` da 26.2): `nav.app-header.navbar.navbar-expand.bg-primary[data-bs-theme="dark"]` (visual basic8 — layout atual usa `bg-body`); toggle sidebar `data-lte-toggle` + `data-controller="sidebar"` + `data-action="click->sidebar#toggle"` (PRESERVA sidebar_controller.js) com `aria-label` pt-BR (RNF03); marca central `@app_icon` + `@app_name` → `root_path`; dropdown de tema fiel ao layout atual (exatamente 1 dropdown — extração limpa, sem duplicação; herda o theme_controller do `<html>`); dropdown do usuário: `nome_completo` (fallback "Visitante" do fonte), email renderizado SOMENTE quando presente (nullable no Frequencia — evita "Não autenticado" enganoso), badges `text-bg-primary` (uma cor p/ todas — convenção registrada, reutilizada na 26.4), logout `button_to logout_path, method: :delete` (RN06) com `turbo_confirm` pt-BR | 1 arquivo de produção criado; zero migrations; auth/ability intactos (RNF06/RN06); layout `admin.html.erb` NÃO tocado (composição é a 26.5) |
| 2026-09-24 (sessão) | Testes: 5 novos em `test/controllers/admin/header_test.rb` — integração que renderiza o partial no `view_context` do controller após action admin REAL (`dashboard_path`): (1) estrutura AdminLTE 4 + marca central + toggle preservando sidebar_controller.js + aria-label pt-BR; (2) dropdown de tema com theme_controller e exatamente 1 dropdown (sem duplicação) + 3 modos light/dark/auto; (3) dropdown do usuário com nome_completo + email + badges `text-bg-primary` por role (sem `bg-primary`); (4) usuário SEM roles nem email → dropdown sem quebrar (zero badges, sem linha de email, logout presente); (5) logout via button_to `logout_path` com `_method=delete` (RN06) + turbo_confirm | 5 runs / 46 asserts / 0 failures; suíte admin 136 runs / 757 asserts / 0 failures |
| 2026-09-24 (sessão) | Validação: `bin/rails zeitwerk:check` (binstub — lição Sprint 23); `bin/rubocop` no arquivo de teste; suíte completa `bin/rails test` | zeitwerk All is good; rubocop 1 file / no offenses (partial `.erb` não lintado — parser Ruby do RuboCop não entende ERB, offenses seriam falsos positivos; mesma prática das 26.1/26.2); suíte 751 runs / 2619 asserts / 1 falha = ÚNICA PRÉ-EXISTENTE de timezone (`presenca_endpoints_test.rb:187`) — sem novas falhas (baseline 746/2573/1) |

#### Tarefa 26.4 — Partial `shared/_title` (breadcrumbs + badges de roles + box ícone 50×50 + auto-"Novo")

- User Story: Como usuário, quero um título de página consistente com breadcrumbs, ícone do recurso e atalho "Novo" automático para saber onde estou e agir rápido
- Rastreabilidade: PRD RF04 (os templates da Sprint 25 renderizam `shared/*` — o título entra na estrutura gerada), RNF05; fontes: `basic8/app/views/shared/_title.html.erb`; depende da gem `breadcrumbs_on_rails` (26.6)
- Estimativa: 3 pontos | Atribuição: Dev A
- Dependências: 26.1 (helpers), 26.3 (convenção badges), **26.6 (gem breadcrumbs + `add_breadcrumb`) — executar 26.6 antes desta na linha do tempo**
- Critérios de aceite:
  - [x] `_title.html.erb` portado com `return if ['sessions', 'dashboard'].include?(controller_name)`
  - [x] Breadcrumbs via `breadcrumbs.any?` (gem) com fallback `@app_name` → `resource_human_name(...)` quando vazio
  - [x] Box ícone 50×50 (`style="width: 50px; height: 50px;"` + `resource_icon(controller_name)`) e `h1` com `resource_human_name(controller_name, action_name)` + `t("helpers.titles.#{action_name}")` quando não-index
  - [x] Badges de roles com a MESMA convenção `text-bg-*` da 26.3 (sem duplicação de decisão)
  - [x] Auto-"Novo": `eval_with_rescue("new_#{controller_name.singularize}_path")` → botão "Novo [recurso]" apenas quando a rota existe (`new_path.present? && new_path != "error"`)
  - [x] Ações: `content_for(:page_actions)` respeitado; `show` renderiza `shared/btn_action_links` da zutils (`labels: false, size: 'sm', hide: ['show']`)
  - [x] Testes: renderização em ação admin real (index com botão Novo quando rota existe; show com btn_action_links; sessions/dashboard não renderizam o título)
  - [x] Chaves I18n `helpers.titles.*` portadas de `basic8/config/locales/pt-BR.yml` (bloco consumido pelo `_title` — `t("helpers.titles.#{action_name}")`/`t("helpers.titles.new")` — não portar de uma vez `helpers.select/submit/links`; fora do escopo das 26.x)
  - [x] `helper ApplicationHelper` na base API (descoberta registrada na Linha do Tempo e nos Riscos: em app API-only o include automático do Rails NÃO roda — recursos 26.1 ficavam invisíveis às views; fix de 1 linha seguindo o precedente da 26.6/R.2)
- Status: 🔵 Em Andamento (implementação concluída e validada — aguardando Code Reviewer/Bug Finder; COMMIT_MODE=manual: commits/MR pendentes de aprovação)

**Linha do Tempo:**

| Horário | O que foi feito | Resultado |
|---------|-----------------|-----------|
| 2026-09-24 (sessão) | Investigação do estado real: fonte `basic8/app/views/shared/_title.html.erb` (87 linhas — `return if ['sessions','dashboard']`, `breadcrumbs.any?` + fallback `@app_name`→`resource_human_name`, box ícone 50×50 `resource_icon`, h1 `t("helpers.titles.#{action_name}")` a menos de index, badges `bg-primary` do fonte, auto-Novo `eval_with_rescue("new_#{plural}_path")` com guard `present? && != "error"`, ações: `content_for?(:page_actions)` → index (botão) → show (`shared/btn_action_links` zutils `labels: false, size:'sm', hide:['show']`)); zutils 4.0.0 (`eval_with_rescue`/`menu_activated?` helpers + partial `_btn_action_links` via engine); locais: Frequencia pt-BR SEM bloco `helpers` (basic8 tem `titles:` create/delete/edit/index/new/save/search/show/update); layout NOVO admin nunca compõe `_title` (26.5); NENHUM controller admin tem action `show` (scaffolds Sprint 25) — adaptação de teste documentada; views admin TODAS setam `content_for :page_title` hoje | Estado confirmado; branch `feat/demanda-port-estilo-basic8-fase1` com working tree das 26.1–26.3/26.6 pendente (COMMIT_MODE=manual) |
| 2026-09-24 (sessão) | Porta do partial `app/views/shared/_title.html.erb` (criado junto ao `_sidebar`/`_header`): `<div class="app-content-header">` íntegro; breadcrumbs admin herdam o crumb "Início" (26.6) com `send(crumb.path)` para Symbol; fallback vazio idem fonte; badges de roles → **`text-bg-primary`** (REUTILIZA a convenção da 26.3 — única mudança visual da porta, registrada no cabeçalho do partial); home `bg-light fw-bold` + `fa fa-home`; box ícone `text-primary bg-primary bg-opacity-10` 50×50 + `resource_icon` (26.1); auto-Novo com `t("helpers.titles.new")` + recurso singularizado; ações com `yield :page_actions`/`btn_action_links` da zutils; `@hide_page_title` preservado do fonte. **Chave I18n**: bloco `helpers.titles.*` adicionado a `config/locales/pt-BR.yml` (port parcial — só o consumido pelo `_title`). **Descoberta (fix de 1 linha)**: `ApplicationHelper` NÃO estava visível nas views — app é `ActionController::API` e o hook `default_helper_module!` (AbstractController::Helpers) só roda na herança DE uma classe com `include ActionController::Helpers`; como a base herda de API, `ApplicationController` nunca recebeu o default (e subclasses resolvem `Admin::ApplicationHelper` → NameError skip). Fix `helper ApplicationHelper` na base (mesmo padrão dos includes explícitos 26.6/R.2 — NOTE documentada no arquivo) | 3 arquivos de produção alterados/criados (2 de produção + locale); zero migrations; auth/ability intactos; contexto API/presença intocado (helper é exposição de métodos — sem nova lógica) |
| 2026-09-24 (sessão) | Testes: 9 novos em `test/controllers/admin/title_test.rb` — integração no padrão 26.2/26.3 (render do partial no `view_context` do controller após action admin REAL; esteira router→before_action→breadcrumbs→CanCan→action): (1) index + breadcrumb "Início" + box 50×50 + h1 + auto-Novo ("Cadastrar Regime") com `regimes_path`; (2) index SEM rota new (`time_records_path`) → sem botão; (3) composição real 26.5 — MESMO view_context renderiza a view real (`admin/regimes/index` → `content_for :page_title` real) e depois o partial → yield preservado sem ícone/h1/auto-Novo (descoberta: `view_context` NÃO é memoizado — actionview-8.0.5/render.rb:109 — cada chamada devolve flow vazio; por isso a composição é simulada no mesmo contexto); (4) `content_for :page_actions` respeitado com prioridade sobre auto-Novo; (5) show via **action real `regimes#edit`** (carrega/autoriza `@regime`) + locals `action_name: "show"` (adaptação documentada — sem show admin real ainda) → `btn_action_links` zutils: Voltar/Editar/Cadastrar + form DELETE e SEM `fa-eye` (hide: ['show']), h1 "Ver Regimes"; (6) badges `text-bg-primary` ×2 (admin+gestor, sem `bg-primary`); (7) user sem roles → zero badges sem quebrar; (8) sessions/dashboard → render blank (return early); (9) fallback breadcrumbs vazio (`locals breadcrumbs: []` — override porque admin real sempre herda o crumb "Início") → `@app_name`→`resource_human_name` | 9 runs / 73 asserts / 0 failures; suíte admin 149 runs / 850 asserts / 0 failures |
| 2026-09-24 (sessão) | Validação: `bin/rails zeitwerk:check` (binstub — lição Sprint 23); `bin/rubocop` nos `.rb` alterados (+ autocorrect de TrailingEmptyLines no teste); suíte completa `bin/rails test` | zeitwerk All is good; rubocop 2 files / no offenses (partial `.erb` não lintado — prática das 26.1–26.3); suíte **764 runs / 2712 asserts / 1 falha** = ÚNICA PRÉ-EXISTENTE de timezone (`presenca_endpoints_test.rb:187` — SincronizarRegistrosPonto: `Time.zone.strptime` de "14:30:45" com `America/Sao_Paulo` devolve 14:30, teste espera 11:30 — parse UTC; causa raiz caracterizada, domínio Presença, fora do escopo das 26.x) — sem novas falhas (baseline 755/2639/1) |

#### Tarefa 26.5 — Refatorar layout `admin.html.erb` para renderizar `shared/*` (título dinâmico + favicon + remoção do JS inline duplicado)

- User Story: Como dev, quero um layout admin enxuto composto pelos partials `shared/*` para que o visual seja único e a manutenção centralizada
- Rastreabilidade: PRD RF14 (flash local preservada), RNF05; fontes: `basic8/app/views/shared/_footer.html.erb` (port simples — incluído nesta tarefa), partials portados 26.2/26.3/26.4
- Estimativa: 2 pontos | Atribuição: Dev A
- Dependências: 26.2, 26.3, 26.4 (partials prontos para composição)
- Critérios de aceite:
  - [x] `admin.html.erb` renderiza `shared/_header`, `shared/_sidebar`, `shared/_title` (quando aplicável) e `shared/_footer` — removendo os blocos inline equivalentes (header/sidebar/footer atuais)
  - [x] **JS inline duplicado da sidebar removido** — persistência continua via `sidebar_controller.js` (Stimulus) + AdminLTE nativo; verificar que o colapso persiste após reload
  - [x] Flash local **preservado (RF14)**: `flash[:notice]`→`alert-success` e `flash[:alert]`→`alert-danger` no layout (sem `shared/flash*`/`bootstrap_flash`)
  - [x] Título dinâmico: `<title>` usa `@app_name` + sufixo da página (`content_for?(:page_title)` ou `resource_human_name`) — sem título hardcoded "API Ponto TJPI"
  - [x] Favicon presente (`link rel="icon"` apontando para asset existente; padrão basic8)
  - [x] Meta `theme-color` ajustada para a paleta `$primary: #2563eb` (sincronizada com a 26.8)
  - [x] Suíte baseline sem novas falhas; verificação HTML real (dashboard/estacoes) com flash, título, menu e badges
- Status: 🔵 Em Andamento (implementação concluída e validada — aguardando Code Reviewer/Bug Finder; COMMIT_MODE=manual: commits/MR pendentes de aprovação)

**Linha do Tempo:**

| Horário | O que foi feito | Resultado |
|---------|-----------------|-----------|
| 2026-09-24 (sessão) | Investigação do estado real: layout admin inline atual (título hardcoded "API Ponto TJPI"; meta theme-color `#007bff`/`#1a1a1a`; SEM favicon; CDN CSS fontsource/admin-lte/overlayscrollbars; sidebar inline com itens reais; **JS inline duplicado nas linhas 293–324** com chave `admin-sidebar-collapsed` + DOMContentLoaded), partials portados 26.2/26.3/26.4 (fonte única; `_sidebar` SEM `data-bs-theme="dark"` — seguindo o fonte basic8, decisão dark registrada), fonte `basic8/shared/_footer.html.erb` (7 linhas), `sidebar_controller.js` (Stimulus — chave `admin-sidebar-collapsed`) + `theme_controller.js`, AdminLTE nativo (`STORAGE_KEY 'lte.sidebar.state'` com `data-enable-persistence`), importmap com admin-lte JS pinado (CDN), `manifest.js` SEM `link_tree ../builds` (build fora de produção), login com layout próprio (`login.html.erb` — admin nunca renderiza "sessions"), views admin TODAS setam `content_for :page_title` (strings puras), flash real: `sessions#create`/`EstacoesController#create` (notice) e `rescue_from CanCan::AccessDenied` (alert) | Estado confirmado; branch `feat/demanda-port-estilo-basic8-fase1` com working tree das 26.1–26.4/26.6 pendente (COMMIT_MODE=manual) |
| 2026-09-24 (sessão) | Refactor do layout `app/views/layouts/admin.html.erb`: composição `render "shared/header"`/`"shared/sidebar"`/`"shared/title"`/`"shared/footer"` com remoção dos blocos inline equivalentes; **JS inline duplicado da sidebar REMOVIDO** (persistência via Stimulus `sidebar_controller.js` + AdminLTE nativo `data-enable-persistence`); flash local RF14 INTACTO (notice→alert-success/alert→alert-danger, sem partial de flash da zutils — o teste RF14 lê o arquivo fonte, comentários evitam citar as strings); título dinâmico `<title>@app_name | sufixo` (`content_for?(:page_title)` com `strip_tags` defensivo ou fallback `resource_human_name`; `yield` NÃO consome o flow — `_title` ainda lê); favicon padrão basic8 (`/icon.png` + `/icon.svg` + apple-touch-icon); meta theme-color light `#2563eb` (paleta `$primary` 26.8) + dark `#1a1a1a` mantido; **CDN CSS admin-lte/overlayscrollbars/fontsource MANTIDO (NOTE no layout)** — build local ainda não entra em produção (26.8) e remover agora quebraria o visual | 1 arquivo de produção alterado + 1 partial criado (`shared/_footer`) + 2 assets `public/icon.png`/`icon.svg` copiados do basic8; zero migrations; auth/ability intactos (RNF06/RN06); contexto API/presença intocado |
| 2026-09-24 (sessão) | Decisões registradas (para Reviewer): (1) **CDN × build local** — CDN CSS não removido (26.8 `link_tree ../builds` substitui); (2) **dark mode do aside** — `data-bs-theme="dark"` do layout inline NÃO portado (o fonte basic8 não tem; a sidebar passa a seguir o tema global do `theme_controller`); (3) **favicon** — port dos assets do basic8 (`icon.svg` círculo placeholder do Rails default; o basic8 repete o svg via `favicon_link_tag` — duplicação do fonte evitada); (4) **theme-color** — light `#2563eb` sincronizado com a 26.8; dark `#1a1a1a` preservado (semântica dark do Frequencia; o basic8 tem uma única meta estática); (5) **footer** — port com identidade TJPI ("Tribunal de Justiça do Piauí" preservado do layout atual) e `Time.current.year` | Nenhuma decisão arquitetural bloqueante; escopo da 26.5 mantido |
| 2026-09-24 (sessão) | Testes: 11 novos em `test/controllers/admin/layout_test.rb` — integração que valida o LAYOUT COMPLETO renderizado (HTML real da resposta; Nokogiri documento completo): (1) composição header/sidebar/footer/title sem blocos inline antigos; (2) dashboard sem `app-content-header` (return early do `_title`) com header/sidebar/footer; (3) título dinâmico em estacoes+dashboard (sem hardcode); (4) flash notice REAL (POST create estação admin → alert-success + botão fechar); (5) flash alert REAL (POST sem permissão → AccessDenied → dashboard → alert-danger); (6) favicon png/svg/apple-touch-icon; (7) theme-color `#2563eb`/`#1a1a1a`; (8) JS inline removido (`refute admin-sidebar-collapsed`/`DOMContentLoaded`) + persistência preservada (`data-controller=sidebar` + `data-enable-persistence`); (9) menu data-driven com destaque ativo; (10) badges `text-bg-primary` (header+title, sem `bg-primary`). **Testes existentes atualizados** (consequência da porta — contrato do título migrou do `<h1>` inline p/ `_title`): `dashboard_controller_test` (2 asserts: sem `.app-content-header` + `<title>` dinâmico) e `time_records_controller_test` (1 assert: `.app-content-header` com texto do `page_title` + `<title>` dinâmico) | 11 runs / 79 asserts / 0 failures no layout_test; suíte admin 160 runs / 929 asserts / 0 failures |
| 2026-09-24 (sessão) | Validação: `bin/rails zeitwerk:check` (binstub — lição Sprint 23); `bin/rubocop` nos 3 `.rb` alterados (+ autocorrect TrailingEmptyLines no layout_test); suíte completa `bin/rails test` | zeitwerk All is good; rubocop 3 files / no offenses; suíte **775 runs / 2796 asserts / 1 falha** = ÚNICA PRÉ-EXISTENTE de timezone (`presenca_endpoints_test.rb:187` — SincronizarRegistrosPonto 11:30 vs 14:30) — sem novas falhas (baseline 764/2712/1) |

### FASE 2 — Assets/build (causa raiz do estilo não reproduzido)

#### Tarefa 26.6 — Gem `breadcrumbs_on_rails` + `add_breadcrumb` no ApplicationController

- User Story: Como dev, quero a gem de breadcrumbs do basic8 para que o `_title` renderize o trilho de navegação na tela admin
- Rastreabilidade: RNF05; fontes: `basic8/Gemfile` (`gem "breadcrumbs_on_rails"`), `basic8/app/controllers/application_controller.rb` (`add_breadcrumb "Início", :root_path`)
- Estimativa: 2 pontos | Atribuição: Dev A
- Dependências: — (independente; sequenciar após 26.1 para evitar conflito de edição no `ApplicationController`)
- Critérios de aceite:
  - [x] `gem "breadcrumbs_on_rails"` no Gemfile + `bundle install` limpo (compatibilidade Rails 8/Ruby 3.4.2 verificada — boot OK)
  - [x] `add_breadcrumb "Início", :root_path` em `ApplicationController` (padrão basic8) — contexto API/presença não afetado (nenhuma view não-admin renderiza breadcrumbs). **Decisão de colocação registrada na Linha do Tempo:** base do Frequencia é `ActionController::API` compartilhada com `Presenca::*` → o `add_breadcrumb` entrou no `Admin::ApplicationController` (precedente ADR-001 Seção 4; RNF01/RNF06)
  - [x] Helper `breadcrumbs` disponível nas views admin (`_title` usa `breadcrumbs.any?` e `crumb.path`/`crumb.name`)
  - [x] `rails zeitwerk:check` OK; suíte baseline sem novas falhas; rubocop limpo
- Status: 🔵 Em Andamento (implementação concluída e validada — aguardando Code Reviewer/Bug Finder; COMMIT_MODE=manual: commits/MR pendentes de aprovação)

**Linha do Tempo:**

| Horário | O que foi feito | Resultado |
|---------|-----------------|-----------|
| 2026-09-24 (sessão) | Investigação do estado real: `breadcrumbs_on_rails` **não** está no Gemfile/Gemfile.lock; base `ApplicationController` = `ActionController::API` (nota ADR-001 Seção 4 — intocado por design; compartilhada com `Presenca::ApplicationController`); `Admin::ApplicationController` com `set_configurations` da 26.1 **intacto**; `root_path` real (`GET /` → `admin/dashboard#index`); **railtie da gem 4.1.0 lido do vendor bundle**: injeta o concern apenas em `ActionController::Base` (gap de app API-only — risco 🟡 do plano; gem NÃO é incompatível com Rails 8/Ruby 3.4: boot/lock/zeitwerk OK) | Estado confirmado; branch `feat/demanda-port-estilo-basic8-fase1` com working tree das 26.1–26.3 pendente (COMMIT_MODE=manual) |
| 2026-09-24 (sessão) | Implementação: `gem "breadcrumbs_on_rails"` no Gemfile (+ comentário Sprint 26/26.6) + `bundle install` (4.1.0 — `railties >= 5.0` satisfeito por Rails 8.0.5); include explícito `BreadcrumbsOnRails::ActionController` na base API (mesmo padrão dos includes `Helpers`/`Flash`/`MimeResponds` já existentes — NOTE documentado); `add_breadcrumb "Início", :root_path` no **`Admin::ApplicationController`** (DECISÃO registrada: como a base é API-only e compartilhada com `Presenca::*`, o breadcrumb é registrado SÓ no contexto admin — padrão basic8 adaptado; RNF01/RNF06 garantem contexto API/presença intocado) | 2 arquivos de produção alterados + Gemfile/Gemfile.lock; zero migrations; auth/ability intactos; contexto API/presença intocado |
| 2026-09-24 (sessão) | Testes: 4 novos em `test/controllers/admin/breadcrumbs_test.rb` — (1) gem carregada + concern no ancestry da base + `helper_method :breadcrumbs`; (2) ação admin REAL (`estacoes_path` — esteira router→before_action→CanCan→action) → cadeia com 1 crumb: name "Início"/path `:root_path` (Symbol — `_title` 26.4 resolve via send, fonte basic8); (3) helper no `view_context` admin (`breadcrumbs.any?` + `crumb.path` → `root_path`) — pré-requisito do `_title`; (4) contexto presença (`presenca_Frequentador` via `Presenca::ApplicationController`, sem sessão) → cadeia VAZIA + zero before_action de breadcrumbs (RNF01) | 4 runs / 20 asserts / 0 failures; suíte admin 140 runs / 777 asserts / 0 failures |
| 2026-09-24 (sessão) | Validação: `bin/rails zeitwerk:check` (binstub — lição Sprint 23); `bin/rubocop` nos 3 `.rb` alterados; suíte completa `bin/rails test` | zeitwerk All is good; rubocop 3 files / no offenses; suíte **755 runs / 2639 asserts / 1 falha** = ÚNICA PRÉ-EXISTENTE de timezone (`presenca_endpoints_test.rb:187` — SincronizarRegistrosPonto horário 11:30 vs 14:30) — sem novas falhas (baseline 751/2619/1) |

#### Tarefa 26.7 — npm `bootstrap-icons` + import no `application.scss`

- User Story: Como dev, quero as classes `bi-*` disponíveis no build CSS para que partials/setores do basic8 que referenciam Bootstrap Icons (ex.: `bi bi-chevron-right` do sidebar fonte, partials zutils das telas da Sprint 25) renderizem ícones
- Rastreabilidade: PRD RF13 (sem zutils.scss — decisão), RNF05; fontes: `basic8/app/assets/stylesheets/application.scss` (`@import "bootstrap-icons/font/bootstrap-icons"`), `basic8/package.json`
- Estimativa: 2 pontos | Atribuição: Dev B (paralela)
- Dependências: — (não interfere com a Fase 1; pode rodar em worktree paralela)
- Critérios de aceite:
  - [x] `yarn add bootstrap-icons` (dependency, não devDependency) — versão compatível com o build Sass
  - [x] `@import "bootstrap-icons/font/bootstrap-icons";` no `application.scss` (posição equivalente ao basic8)
  - [x] WebFonts servidos pelo Propshaft (registrar `node_modules/bootstrap-icons/font/fonts` em `config.assets.paths` se necessário — padrão da task 24.1 do fontawesome; ou copiar para builds)
  - [x] `yarn build:css` compila sem erro; `grep ".bi-chevron-right"` presente em `app/assets/builds/application.css`
  - [x] Convenção documentada: a Fase 1 usa `fa-*` nas partials portadas (consistência Frequencia); o bootstrap-icons cobre resíduos `bi-*` (ex.: nav-arrow do fonte) e partials zutils (Sprint 25) — NÃO reintroduz `bi-*` nas views já migradas para FA
  - [x] Suíte baseline sem novas falhas
- Status: 🔵 Em Andamento (implementação concluída e validada — aguardando Code Reviewer/Bug Finder; COMMIT_MODE=manual: commits/MR pendentes de aprovação)

**Linha do Tempo:**

| Horário | O que foi feito | Resultado |
|---------|-----------------|-----------|
| 2026-09-24 (sessão) | Investigação do estado real: `package.json` do Frequencia (deps: fontawesome-free 6.7.2/admin-lte 4.0.0/bootstrap 5.3.8; scripts `build:css` = sass `--load-path=node_modules` + postcss/autoprefixer → `app/assets/builds/application.css`); `application.scss` atual (variables→bootstrap→admin-lte→fontawesome→custom→dark_mode; SEM bootstrap-icons); `config/initializers/assets.rb` (24.1: `app/assets/builds` + `node_modules/@fortawesome/fontawesome-free/webfonts` nos `assets.paths`); fonte basic8 (`package.json` `bootstrap-icons: ^1.13.1`; `application.scss` linha 7 — `@import "bootstrap-icons/font/bootstrap-icons"` logo após `bootstrap`, antes de `admin-lte`); `manifest.js` SEM `link_tree ../builds` (26.8); ambiente: yarn 1.22.22/node v22.23.0 disponíveis; repo sem branch develop (padrão por demanda) → trilha Dev B em worktree paralela a partir do HEAD consolidado `c51f75c` (skill `parallel-execution`) | Estado confirmado; branch `feat/demanda-port-estilo-basic8-fase2` criada em worktree `Frequencia-fase2` (arquivos disjuntos da Fase 1 — assets/build/config; sem tocar Gemfile/views/helpers da 26.1–26.6); `yarn install` + `bundle install` materializados no worktree |
| 2026-09-24 (sessão) | Implementação: `yarn add bootstrap-icons@^1.13.1` (**dependency**, não devDependency — mesma versão do basic8, compatível com o build Sass 1.97.3); `@import "bootstrap-icons/font/bootstrap-icons";` no `application.scss` na **posição equivalente ao basic8** (após `bootstrap/scss/bootstrap`, antes de `admin-lte` — NOTE documenta a convenção `fa-*` vs `bi-*`); webfonts registrados em `config/initializers/assets.rb` — `node_modules/bootstrap-icons/font/fonts` nos `assets.paths` (padrão 24.1; o SCSS compilado referencia `url("./fonts/bootstrap-icons.woff2?#{hash}")` → servido em `/assets/fonts/...`) | 4 arquivos alterados: `package.json` (+1 dep), `yarn.lock` (+5), `application.scss` (+7 — import + NOTE), `config/initializers/assets.rb` (+6 — NOTE + path); zero migrations; auth/ability intactos (RNF06/RN06); views/partials NÃO tocadas (Fase 1 não é escopo); `$primary` intacto |
| 2026-09-24 (sessão) | Validação do build: `yarn build:css` limpo (EXIT 0 — warnings de deprecação sass pré-existentes do bootstrap 5.3.8, não do bootstrap-icons); `grep ".bi-chevron-right"` → `.bi-chevron-right::before` presente; webfonts `url("./fonts/bootstrap-icons.woff2?24e3...")`/`.woff` presentes no build; `grep 2563eb` → 72 ocorrências (paleta `$primary` preservada — não quebrou o build; validação profunda é a 26.8) | build `app/assets/builds/application.css` regenerado (2.6 MB) |
| 2026-09-24 (sessão) | Testes/validação no worktree (HEAD consolidado): `bin/rails zeitwerk:check`; suíte admin `bin/rails test test/controllers/admin`; suíte completa `bin/rails test`; `bin/rubocop config/initializers/assets.rb` | zeitwerk All is good; suíte admin 122 runs / 578 asserts / 0 failures; suíte completa **731 runs / 2431 asserts / 1 falha** = ÚNICA PRÉ-EXISTENTE de timezone (`presenca_endpoints_test.rb:187` — baseline 731/2431/1 registrada na 26.1) — sem novas falhas; rubocop 1 file / no offenses |
| 2026-09-24 (sessão) | **Convenção registrada (critério):** a Fase 1 usa `fa-*` nas partials portadas (consistência Frequencia — views migradas `bi-*`→`fa-*`); o bootstrap-icons cobre os **resíduos `bi-*`** (ex.: `bi bi-chevron-right` do sidebar FONTE basic8) e partials zutils da Sprint 25 — **NÃO reintroduzir `bi-*` nas views já migradas para FA**. Observações para Reviewer: (1) a trilha Dev B roda worktree a partir do HEAD consolidado (sem as 26.1–26.6 não commitadas de Dev A — baseline de teste bate com a pré-Sprint 26: 731/2431/1); (2) `app/assets/builds/` permanece gitignored (`.keep`/versionamento são tema da 26.8); (3) ruído de ambiente `log/test.log`/`tmp/cache` trackeados ficam FORA do commit da trilha; (4) `bootstrap-icons` reintroduz dependência removida na 24.1 do plano do app — escopo limitado a CSS/classes `bi-*` residuais (risco 🟡 do plano já registrado) | 6 critérios de aceite atendidos; status 🔵 Em Andamento (aguardando Code Reviewer/Bug Finder; COMMIT_MODE=manual: commits/MR pendentes) |

#### Tarefa 26.8 — `link_tree ../builds` no manifest.js + build CSS local servido (paleta `$primary: #2563eb`)

- User Story: Como dev, quero que o CSS compilado localmente (paleta custom) seja servido em produção para que o visual do Frequencia seja o do basic8 (e não o default do CDN AdminLTE)
- Rastreabilidade: PRD RNF05, RF13/D6; fontes: `Frequencia/api-ponto/app/assets/config/manifest.js` (hoje SEM `link_tree ../builds`), `package.json` (`build:css` já existente), `_variables.scss` (`$primary: #2563eb` — já portado no repo)
- Estimativa: 3 pontos | Atribuição: Dev B (paralela)
- Dependências: 26.7 (build final com bootstrap-icons antes da validação); 26.5 (fraca — remoção do CDN admin-lte coordenada)
- Critérios de aceite:
  - [x] `manifest.js` com `//= link_tree ../builds` (o build `app/assets/builds/application.css` passa a entrar no precompile)
  - [x] `RAILS_ENV=production bin/rails assets:precompile` inclui `application.css` do build (verificável no output/`public/assets` do Propshaft)
  - [x] `yarn build:css` limpo; `$primary: #2563eb` presente no CSS servido (grep `2563eb` no build)
  - [x] Ordem CDN→build validada: o layout `admin` não referencia mais o CDN do `admin-lte` (o build local o provê e prevalece); `fontsource`/`overlayscrollbars` CDN avaliados (manter só o necessário; registrar débito se aplicável)
  - [ ] Verificação visual funcional: página admin real com `computed style` primário `#2563eb` (browser dev — sem Capybara) — **PENDENTE manual: sem browser tool nesta sessão; validação estrutural feita (dev server curl do CSS digested 200 + `2563eb`×72 + `.app-sidebar`×85; precompile production sem warnings + URLs reescritas)**
  - [x] Suíte baseline sem novas falhas
- Status: 🔵 Em Andamento (implementação concluída e validada — aguardando Code Reviewer/Bug Finder; COMMIT_MODE=manual: commits/MR pendentes de aprovação)

**Linha do Tempo:**

| Horário | O que foi feito | Resultado |
|---------|-----------------|-----------|
| 2026-09-24 (sessão) | Investigação do estado real: `manifest.js` (4 linhas, SEM `link_tree ../builds`; basic8 tem), `application.scss` (variables→bootstrap→bootstrap-icons(26.7)→admin-lte→fontawesome→custom→dark_mode; cabeçalho obsoleto "app.css/task 24.4"), `package.json` (`build:css` → `app/assets/builds/application.css`; admin-lte ^4.0.0 → instalado **4.9.1** vs CDN 4.0.0), `assets.rb` (24.1 builds+FA webfonts; 26.7 bi `font/fonts`), `_variables.scss` (`$primary: #2563eb` linha 13), layouts: FASE2 = pré-26.5 (inline, 3 CDNs + `stylesheet_link_tag "application"`), FASE1 (worktree) = 26.5 (composição, NOTE linhas 36–40 mantém CDN admin-lte até a 26.8); `.gitignore` (`public/assets/` ignorado linha 9; `builds/*` + `!builds/.keep` linha 16); `public/assets` NÃO existe; admin-lte no node_modules v4.9.1; NENHUM teste referencia CDN (grep exit=1); `login.html.erb`/kiosk com CDN admin-lte (fora do escopo — débito registrado); **Dockerfile + ci.yml + docker-entrypoint SEM `yarn build:css`/`assets:precompile` (gap de deploy — registrar)**; boot production bloqueado nativamente por `Missing secret_key_base` (Devise — sem master.key/RAILS_MASTER_KEY) | Estado confirmado; trilha Dev B em `Frequencia-fase2` (branch `feat/demanda-port-estilo-basic8-fase2`, HEAD `c51f75c`, 26.7 pendente); precompile production só validável via `SECRET_KEY_BASE` efêmero (env) |
| 2026-09-24 (sessão) | Implementação: `manifest.js` += 5ª linha `//= link_tree ../builds`; `app/assets/builds/.keep` criado (tracked via regra `!` — garante o dir no repositório; Propshaft pula dotfiles no precompile); layout `admin.html.erb` (fase2, pré-26.5): link CDN admin-lte **removido** + NOTE ERB 26.8 (build provê AdminLTE — import `admin-lte/src/scss/adminlte`; fontsource/overlayscrollbars mantidos; instrução de consolidação: NO MERGE, remover a MESMA linha do layout 26.5 na Fase 1 e atualizar o NOTE 26.5 nas linhas 36–40); cabeçalho do `application.scss` reescrito (compila para `application.css`, servido via `link_tree ../builds`, CDN admin-lte removido, paleta prevalece) | 3 arquivos de produção alterados + 1 `.keep`; zero migrations; auth/ability intactos; `yarn build:css` EXIT 0 (warnings de deprecação do bootstrap pré-existentes); greps no build: `2563eb`×72, `.bi-chevron-right`×1, banner AdminLTE v4.9.1, `.app-sidebar`×85 |
| 2026-09-24 (sessão) | **ACHADO CRÍTICO — webfonts não resolvem no precompile**: 1ª validação `RAILS_ENV=production bin/rails assets:precompile` → REAL_EXIT=1 (`ArgumentError: Missing secret_key_base` — Devise `devise/rails.rb:41`; sem master.key — comportamento nativo, NÃO é falha da 26.8); retry com `SECRET_KEY_BASE=$(bin/rails secret)` efêmero (env, nenhum arquivo alterado) → REAL_EXIT=0 MAS com **8 warnings "Unable to resolve"** (log `/tmp/opencode/precompile_prod2.log`): `./fonts/bootstrap-icons.woff2/.woff` e `../webfonts/fa-*` → CSS compilado mantém URLs RELATIVAS → em produção browser pede `/assets/fonts/...` e `/webfonts/...` → **404 do @font-face (ícones FA + bi-* quebrados)**. Diagnóstico: load path do Propshaft expõe os webfonts **flat** (`find("bootstrap-icons.woff2")`=ok; `find("fonts/bootstrap-icons.woff2")`=nil; idem `fa-solid-900.woff2` vs `webfonts/...`); precompile compila TODO o load path (as fontes saem flat: `bootstrap-icons-54de1dc5.woff2`, `fa-solid-900-48915711.woff2`); compiler `css_asset_urls.rb` (Propshaft 1.3.2) só reescreve o que `load_path.find` resolve (filename → logical flat); defaults Sass: `$bootstrap-icons-font-dir: "./fonts"` (bootstrap-icons.scss:8) e `$fa-font-path: "../webfonts"` (FA `_variables.scss:52`) — com os quais as URLs **nunca** resolvem | Causa raiz caracterizada; decisão de fix: **override `.` nas duas vars Sass** (abordagem Y — sem mudar `assets.rb`/sem bloat; expõe os arquivos flat já alcançáveis pelas raízes registradas; o padrão basic8 `node_modules/bootstrap-icons/font` como root produziria `fonts/bootstrap-icons.woff2` e só resolveria o bi — o FA do basic8 tem o MESMO mismatch latente `../webfonts`; abordagem X descartada como port fiel porém com precompile de pacote inteiro) |
| 2026-09-24 (sessão) | Fix aplicado no `application.scss`: `$bootstrap-icons-font-dir: ".";` antes do import do bootstrap-icons e `$fa-font-path: ".";` antes do FA (NOTEs pt-BR explicando o porquê — default não resolve contra as raízes flat do load path); NOTE do `assets.rb` (26.7) corrigido (dizia "serve em /assets/fonts/..." — incorreto; não muda a raiz, só o entendimento documental); rebuild `yarn build:css` (EXIT 0) → build com `url("./bootstrap-icons.woff2?24e3...")`/`url("./fa-solid-900.woff2")` | 1 arquivo alterado (`application.scss` +4 linhas NOTE +2 vars) + nota no `assets.rb`; re-precompile production → **REAL_EXIT=0, ZERO "Unable to resolve"**, CSS `application-80fee9e1.css` com `url("/assets/bootstrap-icons-54de1dc5.woff2?24e3...")` e `url("/assets/fa-solid-900-48915711.woff2")` reescritas (digests dos arquivos flat curados no `public/assets`); `2563eb`×72 e `.app-sidebar`×85 no CSS precompilado; manifest `application.css → application-80fee9e1.css` |
| 2026-09-24 (sessão) | Validação estrutural de dev (substituto do browser — sem tool de browser): `bin/rails zeitwerk:check` → All is good; dev server (puma) + curl: `/assets/application-80fee9e1.css` **200** text/css com `2563eb`×72 e `.app-sidebar`×85 (helpers emitem URL **digested** — `asset_path("application.css")` → `/assets/application-80fee9e1.css`); rotas vivas (`/`→302 `/login`; `/up`→200); **resíduo dev registrado**: `/assets/bootstrap-icons.woff2` e `/assets/fa-solid-900.woff2` (URLs **undigested** vindas das `url("./...")` do CSS build) → 404 — `Propshaft::Server#fresh?` exige digest no request (`self.digest == digest || already_digested?`); comportamento ESTRUTURAL do Propshaft dev (também presente no basic8 — port fiel), pré-existente, **sem regressão da 26.8**; produção 100% corrigida pelo fix; resíduo registrado como débito (ícones bi/FA em dev fontes 404 → só migram no browser quando houver verificação visual manual) | zeitwerk OK; suíte admin 122 runs / 578 asserts / 0 failures; suíte completa **731 runs / 2431 asserts / 1 falha** = ÚNICA PRÉ-EXISTENTE de timezone (`presenca_endpoints_test.rb:187` — baseline 731/2431/1, mesma da 26.7); rubocop `config/initializers/assets.rb` → 1 file / no offenses; sem `.rb` novos na 26.8 (partials/`.erb` não lintados — prática das 26.1–26.5) |
| 2026-09-24 (sessão) | Decisões/registros para Reviewer: (1) **consolidação Fase 1 × Fase 2 no merge** — o layout 26.5 da Fase 1 (`Frequencia/api-ponto/app/views/layouts/admin.html.erb`) AINDA tem o link CDN admin-lte + NOTE 26.5 (linhas 36–40): o merge deve remover a linha e atualizar o NOTE (instrução dentro do NOTE ERB do layout fase2); (2) **CDN mantidos**: `fontsource` (Source Sans 3 — fontes não empacotadas no build; `admin-lte/src/scss/_bootstrap-overrides.scss:20` referencia a família) e `overlayscrollbars` CSS (não importado no build — decisão de escopo fica na 26.9; débito 🟢); `login.html.erb`/kiosk seguem com CDN admin-lte (fora do escopo — débito); (3) **gap de deploy**: Dockerfile (template Rails 8) NÃO roda `yarn build:css`/`assets:precompile` e `entrypoint` só faz `db:prepare`; CI (`ci.yml`) não tem job de assets — deploy precisa de `RAILS_MASTER_KEY` + build dos assets (registrar p/ CTO); (4) **produção**: `public/assets` gitignored (sem ruído; re-gerável via `bin/rails assets:clobber`), precompile production nativo bloqueado por segredos ausentes (contornado com env efêmero — sem arquivos alterados); (5) **ruído tracked** `log/development.log`/`log/test.log`/`tmp/cache/bootsnap` (devido aos curls/suíte) — fora do commit da trilha; (6) **verificação visual browser NÃO executada** (sem tool) — critério aberto com nota; estrutura servida integralmente validada por curl | 5 critérios atendidos + 1 pendente manual (browser); status 🔵 Em Andamento (aguardando Code Reviewer/Bug Finder; COMMIT_MODE=manual: commits/MR pendentes) |

#### Tarefa 26.9 — Portar CSS de bibliotecas do basic8 conforme uso real do Frequencia (decisão de escopo)

- User Story: Como dev, quero uma decisão documentada sobre quais CSS de bibliotecas do basic8 entram no build do Frequencia para não carregar CSS morto
- Rastreabilidade: PRD RF13 (não importar zutils.scss), RF12 (bootstrap-table inert), RNF03/RNF05; fontes: `basic8/app/assets/stylesheets/application.scss` (toastr, tom-select, bootstrap-table, fileinput, ckeditor5, overlayscrollbars)
- Estimativa: 2 pontos | Atribuição: Dev B
- Dependências: 26.8 (build local servido — contexto de validação)
- Critérios de aceite:
  - [x] Levantamento das libs do basic8 × uso real do Frequencia (package.json atual: apenas bootstrap/admin-lte/fontawesome; views usam `<select>` nativo — sem tom-select; nenhum toastr/fileinput/ckeditor no Gemfile/views; bootstrap-table inert no PRD RF12)
  - [x] Decisão por biblioteca registrada neste iteration (ou nota no `application.scss`): importar ou não, com justificativa — **default esperado: NENHUM import novo** de toastr/tom-select/bootstrap-table/fileinput/ckeditor5 (fora do escopo real); bootstrap-icons e overlayscrollbars já cobertos nas 26.7/26.10
  - [x] `zutils.scss` NÃO importado (RF13) — confirmado por ausência no `application.scss`
  - [x] Se algum import for adicionado: `yarn build:css` limpo + suíte sem novas falhas; se nenhum: build regenerado sem mudanças e suíte intacta
- Status: 🔵 Em Andamento (implementação concluída e validada — aguardando Code Reviewer/Bug Finder; COMMIT_MODE=manual: commits/MR pendentes de aprovação)

**Linha do Tempo:**

| Horário | O que foi feito | Resultado |
|---------|-----------------|-----------|
| 2026-09-24 (sessão) | Investigação do estado real: fonte `basic8/app/assets/stylesheets/application.scss` (431 linhas — 11 imports de libs: toastr l3, variables l4, bootstrap l5, **bootstrap-icons l7 (26.7)**, **overlayscrollbars l8**, **admin-lte l10 (26.8)**, fontawesome l12–16, **tom-select l18**, **bootstrap-table l20**, **fileinput l22**, **ckeditor5 l23**; + blocos custom dark-mode p/ toastr/tom-select/ck); `basic8/package.json` (deps: fontawesome-free ^7.3.1, bootstrap-fileinput ^5.5.4, bootstrap-table ^1.27.3, ckeditor5 ^48.4.0, overlayscrollbars ^2.14.0, toastr ^2.1.4, tom-select ^2.5.2); `Frequencia/api-ponto` pós-26.7/26.8: `application.scss` (7 imports — variables/bootstrap/bootstrap-icons/admin-lte/FA×5/custom/dark_mode; **ZERO toastr/tom-select/bootstrap-table/fileinput/ckeditor/overlayscrollbars**; NOTE 26.8 linha 26 afirma "CSS do OverlayScrollbars ainda não é importado (decisão de escopo é da 26.9/26.10)"), `package.json` (deps: apenas fontawesome-free ^6.7.2/admin-lte ^4.0.0/bootstrap ^5.3.3/bootstrap-icons ^1.13.1), Gemfile (sem gems de toastr/fileinput/ckeditor/bootstrap-table/tom-select), views (grep `toastr|tom-select|bootstrap-table|fileinput|ckeditor|overlayscrollbars` em app/ = 0 usos em views/JS; únicos matches em comentários SCSS `_custom.scss`/`_dark_mode.scss` que LISTAM o que foi deliberadamente excluído na 24.2/24.3 — + CDN overlayscrollbars no layout admin), forms usam `f.select`/`select_tag` nativos com `form-select` (regimes/_form l20/l26; direitos_deveres/time_records/parcial com select_tag), tabelas `table.table` + kaminari (sem bootstrap-table), `node_modules/` do Frequencia SEM toastr/tom-select/bootstrap-table/fileinput/ckeditor/overlayscrollbars (grep exit=1), `basic8/app/assets/stylesheets/` só tem application.scss + _variables.scss (**zutils.scss não existe no fonte — RF13**), admin-lte scss não importa overlayscrollbars (verificado por grep) | Estado confirmado; ambiente: worktree `Frequencia-fase2` (branch `feat/demanda-port-estilo-basic8-fase2`) com working tree das 26.7/26.8 pendente (COMMIT_MODE=manual); contexto lido (governance/progress/iteration/lessons) |
| 2026-09-24 (sessão) | **Decisão por biblioteca registrada (tabela lib-a-lib abaixo)** — default confirmado: **NENHUM import novo**. **Única alteração de código**: NOTA no cabeçalho do `application.scss` (NOTE 26.9, padrão das 26.7/26.8) documentando a decisão de escopo + as libs excluídas; overlayscrollbars CSS permanece via CDN no layout admin (débito 🟢 — import exigiria `yarn add overlayscrollbars`, dependência nova fora do escopo "nenhum import novo"; o CDN já provê as classes `.os-*` consumidas pelo JS do AdminLTE 4; recomendação registrada: se a 26.10 portar o JS do OverlayScrollbars, o CSS deve entrar no build junto — mesmo pacote npm) | 1 arquivo alterado: `application.scss` (cabeçalho +7 linhas NOTE 26.9); zero migrations; auth/ability intactos (RNF06/RN06); views/partials/layout NÃO tocados (fora do escopo); Gemfile/package.json intocados |
| 2026-09-24 (sessão) | Validação: `yarn build:css` (build regenerado); `bin/rails zeitwerk:check` (binstub — lição Sprint 23); suíte admin `bin/rails test test/controllers/admin`; suíte completa `bin/rails test`; rubocop — NENHUM `.rb` alterado nesta tarefa (SCSS não lintado — prática das 26.1–26.8) | build EXIT 0 (warnings de deprecação sass do bootstrap pré-existentes — mesma assinatura das 26.7/26.8) e **hash md5 idêntico `ecbd177503cdcfbf0864b7f59fdc4d3b`** (build regenerado SEM mudanças — nenhum import novo); zeitwerk All is good; suíte admin 122 runs / 578 asserts / 0 failures (mesma da 26.7/26.8); suíte completa **731 runs / 2431 asserts / 1 falha** = ÚNICA PRÉ-EXISTENTE de timezone (`presenca_endpoints_test.rb:187` — baseline 731/2431/1) — sem novas falhas |

**Tabela de decisão lib-a-lib (26.9):**

| Lib | basic8 `application.scss` | package.json Frequencia | Uso real no Frequencia (evidência) | Decisão | Justificativa |
|-----|---------------------------|-------------------------|-----------------------------------|---------|---------------|
| toastr | `@import "toastr/build/toastr"` (l3) | ausente | nenhum (grep app/ sem match; flash usa alert Bootstrap RF14) | **NÃO importar** | CSS morto — sem flash/toast via toastr |
| tom-select | `@import 'tom-select/dist/css/tom-select.bootstrap4'` (l18) | ausente | nenhum; forms usam `f.select`/`select_tag` nativos (`regimes/_form` l20/l26, `direitos_deveres` l20/l31, `time_records` l23/l30/l137, `parcial` l17–25) | **NÃO importar** | CSS morto — sem `<select>` tom-select; decisão de UX de selects fica p/ Sprint 25 se demandada |
| bootstrap-table | `@import 'bootstrap-table/dist/bootstrap-table'` (l20) | ausente | nenhum; tabelas `table.table` + kaminari/pagy; **inert no PRD RF12** | **NÃO importar** | CSS morto — RF12 não exige; fix `.asc/.desc` do fonte também não portado (24.2) |
| bootstrap-fileinput | `@import 'bootstrap-fileinput/css/fileinput'` (l22) | ausente | nenhum (sem upload de arquivos) | **NÃO importar** | CSS morto — sem uploads no escopo |
| ckeditor5 | `@import 'ckeditor5/dist/ckeditor5'` (l23) | ausente | nenhum (textareas comuns) | **NÃO importar** | CSS morto — sem editor rich text; dark-mode `.ck-*` também não portado (24.3) |
| overlayscrollbars CSS | `@import "overlayscrollbars/styles/overlayscrollbars"` (l8) | ausente (node_modules sem) | **CDN ativo** — `layouts/admin.html.erb` l31 (`overlayscrollbars@2.11.0/styles/overlayscrollbars.min.css`); admin-lte scss NÃO embute o CSS | **NÃO importar no build — CDN permanece como débito 🟢** | Import exigiria dependência npm nova (fora do escopo "nenhum import novo"); CDN provê as classes `.os-*` consumidas pelo JS do AdminLTE 4; **recomendação**: se a 26.10 portar o JS, o CSS entra junto (mesmo pacote npm) |
| bootstrap-icons | `@import "bootstrap-icons/font/bootstrap-icons"` (l7) | ✅ `^1.13.1` (26.7) | import no application.scss l31 + webfonts registrados (26.7) | **Já coberto (26.7)** | — |
| admin-lte | `@import "admin-lte/src/scss/adminlte"` (l10) | ✅ `^4.0.0` | import no build (26.8); CDN removido do layout | **Já coberto (26.8)** | — |
| fontawesome-free | imports l12–16 | ✅ `^6.7.2` | import no build (24.1/26.8) | **Já coberto** | — |
| zutils.scss (RF13) | **não existe** no `basic8/app/assets/stylesheets/` | — (gem zutils 4.0.0 no Gemfile) | ausente no `application.scss` (grep: só comentários) | **NÃO importado (RF13) ✅** | confirmado por ausência; sem port de SCSS da zutils |

> 📌 Registros para Reviewer: (1) a decisão confirma empiricamente o critério "default: NENHUM import novo" — evidência = 0 usos reais em views/JS/Gemfile/package.json; (2) única alteração de código é NOTA documental no `application.scss` (nenhum import adicionado) — build regenerado com hash md5 **idêntico** ao da 26.8 (`ecbd177...` → sem mudanças efetivas); (3) overlayscrollbars CSS permanece CDN como débito 🟢 (decisão registrada AQUI, conforme contexto da 26.8 — questão do CSS resolvida; a 26.10 decide só o JS); (4) CDN fontsource mantido (débito 26.8 — fora do escopo); (5) `login.html.erb`/kiosk com CDN admin-lte (débito 26.8 — fora do escopo); (6) `_custom.scss` l8–11 e `_dark_mode.scss` l21–23 já documentavam (desde 24.2/24.3) a exclusão dessas libs — a 26.9 formaliza/confirma com evidência a decisão de não portar; (7) sem `.rb` alterados → rubocop não aplicável (SCSS não lintado — prática das 26.1–26.8); (8) guia de status 🔵 mantido — aguardando Code Reviewer/Bug Finder |

### FASE 3 — Bibliotecas JS/UX do layout (condicional)

#### Tarefa 26.10 — Tooltips `data-bs-toggle="tooltip"` + OverlayScrollbars JS (decisão: portar ou registrar débito)

- User Story: Como usuário, quero tooltips nos botões de ação e scrollbars customizadas consistentes para uma UX equivalente ao basic8 nas telas geradas
- Rastreabilidade: RNF05; fontes: `basic8/app/javascript/src/functions.js` (init `[data-bs-toggle="tooltip"]` + dispose turbo), `basic8/app/javascript/controllers/application_controller.js` (Stimulus `connect(){ tooltips() }`), `basic8/app/javascript/application.js` (import OverlayScrollbars)
- Estimativa: 2 pontos | Atribuição: Dev A
- Dependências: 26.3/26.4 (tooltips presentes nas partials — via zutils `btn_action_links` no `_title`), 26.8 (build local)
- Critérios de aceite:
  - [x] Port de `tooltips()` (bootstrap.Tooltip em `[data-bs-toggle="tooltip"]` + dispose em `turbo:before-cache`/`turbo:before-visit`) integrado ao entry JS do Frequencia — **via módulo ESM `app/javascript/src/functions.js` importado pelo entry `application.js` com specifier bare pinado no importmap (`pin_all_from "app/javascript/src", under: "src"`)** — o "equivalente compatível com importmap — sem esbuild" do critério (a via Stimulus foi avaliada e descartada: Stimulus NÃO está no runtime — importmap sem pins de stimulus/turbo; decisão detalhada na Linha do Tempo)
  - [ ] Tooltips inicializam nas telas admin reais (verificação browser — sem Capybara) — **PENDENTE manual: sem browser tool nesta sessão; validação estrutural feita (mapa importmap com `src/functions` + entry servido com o import + asset `src/functions-*.js` 200 text/javascript + elementos `[data-bs-toggle="tooltip"]` presentes no HTML admin real via zutils `btn_action_links`)**; sem duplicação com o `bootstrap` já pinado no importmap (uso do global `window.bootstrap.Tooltip` exposto pelo bundle UMD — pin único preservado)
  - [x] OverlayScrollbars JS: **DÉBITO TÉCNICO 🟢** (NÃO portado — `yarn add overlayscrollbars` não executado; justificativa na Linha do Tempo; CSS permanece via CDN conforme 26.9; muda recomendação da 26.9: CSS NÃO entra no build nesta task porque o JS não foi portado — registrado)
  - [x] Decisão registrada no relatório da tarefa (portado vs débito + motivo) — Linha do Tempo abaixo + resumo de entrega
  - [x] Suíte baseline sem novas falhas
- Status: 🔵 Em Andamento (implementação concluída e validada — aguardando Code Reviewer/Bug Finder; COMMIT_MODE=manual: commits/MR pendentes de aprovação)

**Linha do Tempo:**

| Horário | O que foi feito | Resultado |
|---------|-----------------|-----------|
| 2026-09-24 (sessão) | Investigação do estado real: fontes basic8 (`functions.js` 16 linhas — `import * as bootstrap`, `tooltips()`/`disposeTooltips()` + listeners turbo; `application_controller.js` Stimulus `connect(){ tooltips() }`; `application.js` esbuild com `import { OverlayScrollbars }` sem init explícito — o init real vem do pacote npm `@zezim/zutils/init-admin-lte`); Frequencia: importmap com 4 pins (application/jquery/bootstrap UMD/admin-lte) SEM stimulus/turbo → entry `application.js` (3 imports side-effect) NÃO importa `./controllers` → **controllers Stimulus (sidebar/theme/hello/username_preview) INERTES no runtime (gap pré-existente Sprint 24 — registrado p/ Reviewer/CTO)**; AdminLTE JS (CDN 4.0.0 e node_modules 4.9.1) SEM referência a OverlayScrollbars (grep 0); zutils gem SEM módulos JS no Frequencia (npm zutils só no basic8); greps: NENHUM `data-bs-toggle="tooltip"` nas views atuais (tooltips virão da Sprint 25 via `btn_action_links` zutils) | Estado confirmado; branch `feat/demanda-port-estilo-basic8-fase1` (working tree 26.1–26.6 pendente, COMMIT_MODE=manual); repo sem branch develop (padrão por demanda — inicialização adaptada: continuação na branch da demanda, sem commits) |
| 2026-09-24 (sessão) | **DECISÃO 1 — Via de integração dos tooltips (Stimulus vs equivalente importmap):** a via Stimulus do fonte (controller `application` + `data-controller="application"` no layout) exigiria pinar `@hotwired/stimulus` + `@hotwired/stimulus-loading` e ligar `./controllers` no entry — ATIVANDO de uma vez os 4 controllers hoje inertes (mudança de runtime sistêmica, fora do escopo da 26.10, sem browser p/ validar). **DECISÃO: equivalente compatível com importmap** — módulo ESM puro `app/javascript/src/functions.js` (porta fiel: `tooltips()`/`disposeTooltips()` + listeners turbo; adaptação `window.bootstrap.Tooltip` porque o bundle UMD pinado NÃO expõe named exports ES e define o global ao ser avaliado; init no top-level = equivalente do `connect()`; exports permanecem p/ futura integração Stimulus Sprint 25) + `import "src/functions"` no entry + `pin_all_from "app/javascript/src", under: "src"` no importmap (padrão nativo p/ módulos locais, SEM esbuild e SEM duplicar o pin bootstrap — critério "sem duplicação" atendido). Dispose turbo fica portado (no-op sem Turbo — cada visita é full reload) | 3 arquivos de produção alterados: `app/javascript/src/functions.js` (NOVO), `app/javascript/application.js` (+1 import c/ NOTE), `config/importmap.rb` (+1 pin_all_from c/ NOTE); zero migrations; auth/ability intactos (RNF06/RN06); contexto API/presença intocado; layout admin NÃO tocado (sem data-controller) |
| 2026-09-24 (sessão) | **ACHADO — resolver Static do Propshaft quebrava o pin em dev:** após criar o pin, `javascript_importmap_tags` NÃO emitia `src/functions` (runner + dev server) — causa: `path_to_asset("src/functions.js")` → `Propshaft::MissingAssetError`; o resolver usado era **Static** (manifest de `public/assets/.manifest.json` gerado pelo precompile production da 26.8) e o manifest não tinha o asset novo; `assets.load_path.find` achava o arquivo (prova de que o load path estava correto — resolver errado). **FIX: `bin/rails assets:clobber`** (canônico — `public/assets` gitignored e re-gerável, prática já registrada na 26.8) → dev volta ao resolver Dynamic → mapa com `"src/functions": "/assets/src/functions-a6aa16aa.js"` | Clobber sem efeitos colaterais (suíte não depende de public/assets); resíduo do precompile da 26.8 removido; registro p/ Reviewer: manter `public/assets/` ausente em dev (senão importmap resolve contra o manifest stale) |
| 2026-09-24 (sessão) | **DECISÃO 2 — OverlayScrollbars JS: DÉBITO TÉCNICO 🟢** (NÃO portar). Evidências: (1) telas atuais/pilotos (Sprint 25) NÃO dependem — grep overlayscrollbars em `app/` = só o CDN CSS no layout admin (26.5) e nota 26.9; zero uso em views/JS; (2) AdminLTE JS não integra (grep 0 em dist/src); (3) o init real no basic8 vem do pacote npm `@zezim/zutils` (inexistente no Frequencia — só a gem); (4) portar exigiria `yarn add overlayscrollbars` + import no entry via pin CDN/JS + definir o alvo do init (sem fonte do init no Frequencia — seria invenção) + mover o CSS do CDN pro build (recomendação 26.9) — cadeia fora do escopo e sem verificação browser. **Consequência sobre a recomendação da 26.9 ("se o JS for portado, o CSS entra no build junto"): como o JS NÃO foi portado, o CSS permanece no CDN** (nada muda) | Débito registrado; sprint não bloqueada; nenhuma dependência npm nova (package.json/yarn.lock intocados) |
| 2026-09-24 (sessão) | Testes: 5 novos em `test/controllers/admin/tooltips_test.rb` — contrato estrutural da porta (padrão 26.5 de leitura de fonte + integração real): (1) módulo expõe `window.bootstrap.Tooltip` + `[data-bs-toggle="tooltip"]` + dispose em turbo:before-cache/visit + init top-level; (2) entry importa `src/functions` DEPOIS de bootstrap (ordem do global); (3) importmap com 1 pin único de bootstrap + pin_all_from src + módulo sem imports próprios (zero deps); (4) HTML admin REAL (regimes#edit + `shared/_title` show — padrão 26.4) → `btn_action_links` zutils gera ≥3 disparadores `[data-bs-toggle="tooltip"]` com `data-bs-title` (alvo que o tooltips() inicializa nas telas Sprint 25); (5) layout admin renderizado emite importmap tags com `application` e `src/functions` (entry carregado) | 5 runs / 37 asserts / 0 failures |
| 2026-09-24 (sessão) | Validação: `bin/rails zeitwerk:check` (binstub — lição Sprint 23); `bin/rubocop test/controllers/admin/tooltips_test.rb` (+autocorrect TrailingEmptyLines); suíte completa `bin/rails test`; dev server (puma 3010) + curl: `/login` 200, entry `application-7ca32732.js` servido com `import "src/functions"`, asset `src/functions-a6aa16aa.js` 200 text/javascript com o conteúdo do módulo; mapa importmap com o pin (validação em admin autenticado coberta pelo teste 5) | zeitwerk All is good; rubocop 1 file / no offenses (JS/ERB não lintados — prática das 26.1–26.8); suíte **780 runs / 2833 asserts / 1 falha** = ÚNICA PRÉ-EXISTENTE de timezone (`presenca_endpoints_test.rb:187`) — sem novas falhas (baseline 775/2796/1); puma encerrado sem erros |
| 2026-09-24 (sessão) | Decisões/registros para Reviewer: (1) **gap pré-existente (Sprint 24)**: importmap sem pins de stimulus/turbo + entry sem `./controllers` → controllers Stimulus inertes; a 26.10 NÃO ativa o Stimulus (fora do escopo; sem browser); reportar p/ CTO/Orchestrator — tema é pré-requisito de UX runtime da Sprint 25 (theme/sidebar também são afetados — embora a persistência da sidebar funcione via AdminLTE nativo `data-enable-persistence`); (2) **resíduo do precompile 26.8 (`public/assets/.manifest.json`) quebrava a resolução do importmap em dev** — clobber aplicado; manter dev SEM manifest; (3) **bootstrap consumido via global `window.bootstrap`** (bundle UMD) — sem duplicação do pin; se a Sprint 25 precisar de named exports ES do bootstrap, será necessário trocar o pin para o build ESM (`bootstrap.esm.min.js`) — registrar como decisão futura; (4) **verificação visual browser NÃO executada** (sem tool) — critério 2 aberto com nota "verificação manual pendente"; validação estrutural integral (mapa + entry + asset + HTML real); (5) OverlayScrollbars JS = débito 🟢 (decisão 2 acima); CSS `.os-*` segue via CDN (26.9) | 4 critérios atendidos + 1 pendente manual (browser); status 🔵 Em Andamento (aguardando Code Reviewer/Bug Finder; COMMIT_MODE=manual: commits/MR pendentes) |
| 2026-09-24 (consolidação) | **Trilhas Fase 1 (26.1–26.6, 26.10; worktree `Frequencia`, branch fase1) e Fase 2 (26.7–26.9; worktree `Frequencia-fase2`) nunca haviam sido mescladas.** Sintoma no browser: ícones Font Awesome como tofu (caixas com hex) — o layout da Fase 1 servia build CSS com `url("../webfonts/fa-solid-900.woff2")` → 404 (fix `$fa-font-path: "."`/`$bootstrap-icons-font-dir: "."` da 26.8 só existia na Fase 2). Relato inicial "badges não carregam" era falso-positivo: badges de role renderizavam (HTML autenticado como admin.admin). Ação: 33 arquivos da Fase 1 trazidos para a Fase 2 (layout: CDN admin-lte 4.0.0 removido; NOTE 26.5 substituída pela NOTE 26.8); rebuild; suíte **780 runs / 2833 asserts / 1 falha** (timezone pré-existente `presenca_endpoints_test.rb:187`). Falha transitória `tooltips_test.rb:165` causada por `public/assets/.manifest.json` stale (precompile 26.8) → `assets:clobber` (reincidência da lição da 26.10). Commit único `1a73a4d` (feat, 26.1–26.10) + push para origin (COMMIT_MODE=manual, aprovado pelo usuário). Worktree Fase 1 descartada; pasta `Frequencia` agora na branch fase2; branch fase1 apagada localmente e no origin (2026-09-24, aprovado pelo usuário). Verificação ao vivo (puma :3000): `fa-solid-900-*.woff2` e `bootstrap-icons-*.woff2` 200; AdminLTE servido pelo build (4.9.1) em vez do CDN 4.0.0 | Implementação consolidada; pendentes: Code Reviewer, Bug Finder, verificação visual final no browser (usuário), critérios browser 26.8/26.10, MR não aberto |

## Tabela de estimativas e dependências

| Tarefa | Título | Pontos | Fase | Dev | Depende de |
|--------|--------|--------|------|-----|------------|
| 26.1 | Helpers de contexto + fallback `ApplicationRecord.icon` | 2 | 1 | A | Sprint 24 concluída |
| 26.2 | Partial `shared/_sidebar` (menu data-driven) | 3 | 1 | A | 26.1 |
| 26.3 | Partial `shared/_header` (marca central + badges) | 2 | 1 | A | 26.1 |
| 26.4 | Partial `shared/_title` (breadcrumbs + auto-Novo) | 3 | 1 | A | 26.1, 26.3, 26.6 |
| 26.5 | Refactor `admin.html.erb` (composição `shared/*`) | 2 | 1 | A | 26.2, 26.3, 26.4 |
| 26.6 | Gem `breadcrumbs_on_rails` + `add_breadcrumb` | 2 | 2 | A | após 26.1 (conflito AppController) |
| 26.7 | npm `bootstrap-icons` + import SCSS | 2 | 2 | B | — (paralela) |
| 26.8 | `link_tree ../builds` + build local + paleta `#2563eb` | 3 | 2 | B | 26.7; 26.5 (fraca) |
| 26.9 | Decisão de escopo CSS de bibliotecas | 2 | 2 | B | 26.8 |
| 26.10 | Tooltips + OverlayScrollbars JS (ou débito) | 2 | 3 | A | 26.3, 26.4, 26.8 |

**Total: 10 tarefas | 23 pontos | 1 dev (2 com Dev B) | 2 semanas (1 semana com 2 devs)**

## Caminho crítico

```
26.1 → 26.3 → 26.6 → 26.4 → 26.5 → 26.8 → 26.9 → 26.10
```

- **26.2** pode iniciar logo após a 26.1 (paralela com 26.3 na mesma branch ou worktree).
- **26.7** é independente e pode rodar em worktree paralela desde o HEAD consolidado.
- **26.8** depende fracamente da 26.5 (remoção do CDN admin-lte coordenada — não bloqueia início).
- Com Dev B: Fase 2 (26.7/26.8/26.9) em worktree própria; Dev A segue 26.1→26.3→26.6→26.4→26.5→26.10.

## Riscos

- 🟡 **Dois trilhos de planejamento**: parte da Fase 2 (build local, `$primary: #2563eb`, helpers já portados, theme/sidebar controllers) consta como concluída no `Frequencia/SPRINT-PLAN.md` → as tarefas iniciam verificando o estado real (sem duplicar trabalho concluído; diferença entre trilhos é insumo do Code Reviewer/CTO).
- 🟡 **Conflito de edição**: 26.1 × 26.6 tocam `ApplicationController` (e 26.5 × 26.8 o layout admin) → mesmo dev ou sequenciar; com 2 devs, arquivos disjuntos (ver skill `parallel-execution`).
- 🟡 **`breadcrumbs_on_rails` é gem antiga**: compatibilidade Rails 8/Ruby 3.4 — se incompatível, fallback = breadcrumbs manuais no `_title`; reportar sem reabrir decisões ADR/D1-D6.
- 🟡 **`bootstrap-icons` reintroduz dependência removida na 24.1 do plano do app**: escopo limitado a CSS/classes `bi-*` residuais e partials zutils (Sprint 25); não toca views já migradas para FA.
- 🟡 **`link_tree ../builds` ausente hoje** → build fora de produção: corrigir e validar precompile; risco de ordem CDN×build (paleta `#2563eb` vs default `#007bff`) — remover CDN admin-lte.
- 🟢 **Baseline 1 falha pré-existente** de timezone (`presenca_endpoints_test.rb:187`) — aceite = sem NOVAS falhas (731/2431/1).
- 🟢 **Sem Capybara/Selenium** — verificação visual via browser manual + testes estruturais (padrão do projeto).
- 🟢 **Convenção badges `text-bg-*`** decidida na 26.3 e reutilizada na 26.4 (registrada para não divergir).
- 🟢 **App API-only NÃO expõe `ApplicationHelper` às views por padrão** (descoberta na 26.4): o hook `default_helper_module!` do AbstractController::Helpers só roda na herança de classe que já inclui `ActionController::Helpers` — como a base herda de `ActionController::API`, o default nunca disparou (e subclasses resolvem `Admin::ApplicationHelper` → skip silencioso). Fix canônico aplicado: `helper ApplicationHelper` na base (precedente 26.6/R.2 — includes explícitos para hooks de Base que não alcançam API). Observação para Code Reviewer: sem esse fix, as 26.2/26.3 passavam porque `menu_activated?`/`eval_with_rescue` também existem na zutils (engine inclui globalmente) — a 26.4 foi a primeira partial a depender EXCLUSIVAMENTE de métodos só-ApplicationHelper.
- 🟡 **Numeração/ordem**: esta sprint (26/estilo) é PRELÚDIO da Sprint 25 (scaffolds) do implementation_plan; o Gantt do plano (Sprints 25–28) aguarda revisão do Project Planner (renumerar piloto para 27+ ou registrar precedência) — reportado ao Orchestrator.

- 🔴→🟢 **Trilhas paralelas não consolidadas (materializado e mitigado em 2026-09-24)**: Fase 1 e Fase 2 validadas isoladamente; o fix de fontes da 26.8 não chegava ao layout da 26.5 → ícones tofu. Mitigado pela consolidação no `1a73a4d`. Residual resolvido: branch `feat/demanda-port-estilo-basic8-fase1` apagada também no origin (2026-09-24, aprovado pelo usuário) — MR sai só da fase2.
- 🟡 **Validação visual ainda manual**: critérios browser das 26.8/26.10 e verificação final do usuário pendentes; a suíte não detecta 404 de webfont (sem Capybara/Selenium).
- 🟡 **Débitos registrados (não bloqueiam o aceite da 26)**: Stimulus inerte (importmap sem pins stimulus/turbo — pré-requisito de UX runtime da Sprint 25); OverlayScrollbars JS; gap de deploy (Dockerfile/CI sem etapa de build CSS/`assets:precompile` verificada); CDN residual nos layouts login/kiosk.

## Definição de Pronto

> Estado em 2026-09-24 (pós-consolidação `1a73a4d`). `[x]` = verificado por teste/HTML/HTTP; `[~]` = implementado, verificação manual/revisão pendente.

- [x] Partials `shared/_header`, `shared/_title`, `shared/_sidebar`, `shared/_footer` portados e renderizados pelo layout admin; JS inline duplicado da sidebar removido; título dinâmico + favicon
- [x] Helpers `resource_human_name`/`resource_icon`/`set_configurations` + `ApplicationRecord.icon` (fallback) testados
- [x] Convenção badges `text-bg-*` documentada e aplicada (header + title) — badges confirmados no HTML autenticado
- [x] `breadcrumbs_on_rails` no Gemfile + `add_breadcrumb`; `bootstrap-icons` no build; `link_tree ../builds` no manifest; paleta `$primary: #2563eb` servida (CDN admin-lte removido; AdminLTE 4.9.1 do build; webfonts FA/BI 200)
- [x] Decisão de escopo da 26.9 registrada (default: nenhum CSS morto importado; `zutils.scss` NÃO importado — RF13)
- [~] Fase 3: tooltips estruturalmente validados (teste); verificação browser pendente; OverlayScrollbars JS = débito registrado
- [x] Suíte sem NOVAS falhas — baseline atualizado pós-consolidação: **780 runs / 2833 asserts / 1 falha** (timezone pré-existente); [~] `rails zeitwerk:check`/rubocop a reconfirmar no Code Reviewer sobre o `1a73a4d`
- [~] Zero migrations e auth/ability intactos (RNF06/RN06) — a confirmar no Code Reviewer
- [x] Rastreabilidade PRD (RF04/RF05/RF06/RF12/RF13/RF14, RNF01/RNF03/RNF05/RNF06, D1/D6) + fontes basic8 por tarefa
- [ ] Code Reviewer + Bug Finder executados sobre o commit consolidado `1a73a4d` (relatórios em `docs/quality/`)
- [ ] Verificação visual final no browser pelo usuário (ícones FA/BI, paleta, badges, tooltips)
- [ ] MR aberto a partir de `feat/demanda-port-estilo-basic8-fase2` (nunca da fase1)

## Sincronização CAPTEI (opcional — sob demanda)

> ⚠️ **Não executada nesta sessão:** a skill `captei-sync-protocol` existe em `ai-workflow/src/skills/captei-sync-protocol/SKILL.md` mas **não está registrada** no registry de skills da sessão e **não há MCP tools do CAPTEI** disponíveis. Se o Orchestrator/usuário dispuser do ambiente CAPTEI, executar o espelhamento das apt-tasks abaixo (padrão da iteration_24).

| Apt-tasks | Atribuição | Tipo | Complexidade |
|-----------|------------|------|--------------|
| 26.1 | Dev A | Backend | Média |
| 26.2 | Dev A | Frontend | Alta |
| 26.3 | Dev A | Frontend | Média |
| 26.4 | Dev A | Frontend | Alta |
| 26.5 | Dev A | Frontend | Média |
| 26.6 | Dev A | Backend | Média |
| 26.7 | Dev B | Frontend | Baixa |
| 26.8 | Dev B | Frontend/Assets | Média |
| 26.9 | Dev B | Análise | Baixa |
| 26.10 | Dev A | Frontend | Média |